sudo: false

language: node_js

node_js:
  - "4"
  - "5"
  - "6"
  - "7"

install:
  - npm install -g bower
  - node --version
  - npm --version
  - git --version
  - bower --version
  - npm install

after_success:
  - npm run coveralls

os:
  - linux
