sudo: false

language: node_js

node_js:
  - "4"
  - "5"
  - "6"
  - "7"

install:
  - npm install -g grunt-cli bower
  - node --version
  - npm --version
  - git --version
  - bower --version
  - npm install

os:
  - linux

script:
  - grunt CI
